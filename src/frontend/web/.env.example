# Copy to .env.local (gitignored) for local development.
# Use the same origin as your Colab notebook prints (ngrok Public URL), no trailing path.
# Example:
# VITE_API_URL=https://your-subdomain.ngrok-free.app

VITE_API_URL=
